type Mutation {
  login(email: String!, password: String!): User
}

type Query {
  auth: User
}

type User {
  id: ID!
  email: String!
  username: String!
  password: String!
  token: String
  id_UserType: Int
}
